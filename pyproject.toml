[tool.poetry]
name = "ppi"
version = "0.1.0"
description = ""
authors = ["justin <justinjagoss@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.3.4"
pytest-cov = "^6.0.0"
pandas = "^2.2.3"
requests = "^2.32.3"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "black>=22.0",
    "isort>=5.0",
    "mypy>=1.0",
    "ruff>=0.1.0",
]
docs = [
    "sphinx>=7.0",
    "sphinx-rtd-theme>=1.0",
]

[project.urls]
Homepage = "https://github.com/yourusername/my-ppi-package"
Documentation = "https://my-ppi-package.readthedocs.io"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"